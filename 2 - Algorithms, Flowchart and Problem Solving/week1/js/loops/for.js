const greeting = "hello world";
const list = [1, 2, "hello", false, [2, 4]];
const arr = [];

const string1 = "a lazy dog outside";

for(let x = 0; x < greeting.length; x++){
    arr.push(greeting.charAt(x));
}
// console.log(arr);

